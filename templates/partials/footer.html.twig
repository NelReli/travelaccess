<footer class="bd-footer py-4 py-md-5 mt-5 bg-footer">
    <div class="container px-4 px-md-3 text-body-tertiary">
        <div class="row d-flex justify-content-between">
            {# Partie gauche #}
            <div class="col-lg-3 mb-3">
                <a class="navbar-brand" href="{{ path('app_home') }}">
                    <img src="{{ asset('images/Logo_fond_fonce.png') }}" alt="Logo du site sur fond foncé" style="height: 50px;">
                </a>
                <p class="mt-3 col text-white">Découvrez le monde accessible grâce aux expériences partagées par notre communauté de voyageurs passionnés.</p>
            </div>
            {# Partie centrale #}
            <div class="col-6 col-lg-2 mb-3 ">
                <h5 class="text-white">Navigation</h5>
                <ul class="list-unstyled">
                    <li class="mb-2"><a class="text-white text-decoration-none" href="{{ path('app_home') }}">Accueil</a></li>
                    <li class="mb-2"><a class="text-white text-decoration-none" href="{{ path('app_about') }}">À propos</a></li>
                </ul>
            </div>
            {# Partie droite #}
            <div class="col-6 col-lg-2 mb-3">
                <h5 class="text-white">Communauté</h5>
                <ul class="list-unstyled">
                    <li class="mb-2 text-white list-group-item d-flex justify-content-between align-items-center">Histoires partagées<span class="badge fw-bold fs-6" aria-label="{{ articleCount }} histoires partagées">{{ articleCount }}</span></li>
                    <li class="mb-2 text-white list-group-item d-flex justify-content-between align-items-center">Membres<span class="badge fw-bold fs-6" aria-label="{{ userCount }} membres inscrits">{{ userCount }}</span></li>
                    <li class="mb-2 text-white list-group-item d-flex justify-content-between align-items-center">Destination<span class="badge fw-bold fs-6" aria-label="{{ cityCount }} destinations">{{ cityCount }}</span></li>
                </ul>
            </div>
        </div>
        {# Partie droits réservés #}
        <div class="row pt-4 border-top border-info-subtle text-center text-md-start">
            <div class="col-12 col-md-6">
                <p class="text-white mb-2">© 2025 TravelAccess. Tous droits réservés.</p>
            </div>
            <div class="col-12 col-md-6">
                <ul class="list-unstyled d-flex flex-column flex-sm-row justify-content-center justify-content-md-end">
                    <li><a class="text-white ms-2" href="{{ path('app_terms') }}">Conditions d'utilisation</a></li>
                    <li><a class="text-white ms-2" href="{{ path('app_mentions') }}">Politique de confidentialité</a></li>
                    <li><a class="text-white ms-2" href="{{ path('app_contact') }}">Contact</a></li>
                </ul>
            </div>
        </div>


        
    </div>
</footer>