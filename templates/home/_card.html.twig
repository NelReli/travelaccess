                <div class="col-md-5">
                    <div class="gallery">
                        {% for image in article.images|slice(0, 6) %}
                            <div class="position-relative">
                                <img class="gallery-img" src="{{ asset('images/articles/' ~ image.getName) }}" alt="Image galerie {{ image.getName }}" />
                                    {% if loop.index == 6 and article.images|length > 6 %}
                                        <div class="position-absolute gallery-img top-0 start-0 w-100 h-100 d-flex align-items-center justify-content-center bg-dark bg-opacity-50">
                                            <div class="text-white fw-bold fs-4">
                                                <span class="fw-bold">+ {{ article.images|length - 6 }}</span>                                          
                                            </div>
                                        </div>
                                    {% endif %}
                            </div>
                        {% endfor %}
                    </div>
                </div>

                {# Partie Descriptif d'Article #}
                <div class="col-md-7 align-self-end ">
                    <div class="d-flex align-items-baseline">
                        <p class="mb-0 me-3 mt-3" >Accessibilité :</p>
                        <div>
                            {% for i in 1..5 %}
                                {% if i <= article.rating %}
                                    <span class="fs-5 text-warning" >&#9733;</span> 
                                {% else %}
                                    <span class="fs-5 text-secondary">&#9733;</span>
                                {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                    <div class="small">
                        <span>{{ article.city|title }}, {{ article.country|upper }}</span>
                        <span>|</span>
                        <span>{{ article.createdAt|date('d.m.Y') }}</span>
                    </div>
                    <h2 class="my-2 fs-3"><a class="text-decoration-none title_article" href="{{ path('app_article_show', {'slug': article.slug, 'id': article.id}) }}">{{ article.title|upper }}</a></h2>
                    <p class=" mb-2">{{ article.description|slice(0, 300) ~ '...' }}</p>
                    <div class="d-flex justify-content-between align-items-end">
                        <div>
                            <img class="rounded-circle" src="{{ asset('images/avatars/' ~ article.author.avatar) }}" alt=" avatar de l'utilisateur {{ article.author.username }}" height="40" width="40" />
                            <span class="align-bottom"><small> Publié par <strong><a class="text-reset text-decoration-none" href="{{ path('app_profile_show', {'username':  article.author.username}) }}">{{ article.author.username|title }}</a></strong></small></span>
                        </div>
                        <div class="d-flex gap-3">
                            <div class="d-flex gap-3">
                                <span class="badge text-dark" aria-label="{{ article.views }} vues"><i class="fa-regular fa-eye me-1" aria-hidden="true"></i> {{ article.views }}</span>
                                <span class="badge text-dark" aria-label="{{ commentCounts[article.id]|default(0) }} commentaires"><i class="fa-regular fa-comment me-1" aria-hidden="true"></i> {{ commentCounts[article.id]|default(0) }}</span>
                            </div>
                        </div>
                    </div>
                </div>
                
